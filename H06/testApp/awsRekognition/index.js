exports.handler = async (event, context) => {

    // Input: bucket ID of extracted frames and name of
    // Extract frames of video and save to s3 bucket

    return {"humanSentence": "Hello I'm definitely human!"};
};
