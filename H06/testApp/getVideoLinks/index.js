exports.handler = async (event, context) => {

    // Input: String of video bucket ID
    // TODO: Extract video links and save to array for extractFramesParallel

    return {"videoLinks": ["http1", "http2"], "numberOfVideos": 2};
};
